# copy a file to remote server
scp test.txt  remoteuser@remotehost:/remote/directory

# copy a file from remote server to current dir.
scp user@remotehost:/home/user/test.txt .

ln -s target_file link_file